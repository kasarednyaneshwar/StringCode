package stringcode;

public class StringCharacterArray
{
    public static void main(String[] args)
    {
       // char[] password=new char[]{'k','m','n'};
       //  String password1=new String(password);
       //  System.out.println(password1);
        //Question.
        // wy character array is better than string to save a password
        //Ans.caracter array print the objcet and string print the name of the inside the object.
        char[] password2=new char[]{'k','m','n'};
        String password3=new String("kmn");
        System.out.println(password2);
        System.out.println(password3);
    }
}
